<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ggram_black">

    <!-- MAIN CHAT CONTENT -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/ggram_surface"
            app:elevation="0dp">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/main_toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/ggram_surface">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/btn_open_drawer"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:padding="4dp"
                        android:src="@android:drawable/ic_menu_sort_by_size"
                        app:tint="@color/ggram_text_primary"
                        android:contentDescription="Menu" />

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="16dp"
                        android:layout_weight="1"
                        android:text="Ggram"
                        android:textColor="@color/ggram_text_primary"
                        android:textSize="20sp"
                        android:textStyle="bold" />

                    <!-- Mark all read action button -->
                    <ImageView
                        android:id="@+id/btn_mark_all_read"
                        android:layout_width="36dp"
                        android:layout_height="36dp"
                        android:padding="6dp"
                        android:src="@drawable/ic_check_sleek"
                        android:contentDescription="Mark All Read"
                        android:layout_marginEnd="8dp" />

                    <!-- Search button -->
                    <ImageView
                        android:id="@+id/btn_search"
                        android:layout_width="36dp"
                        android:layout_height="36dp"
                        android:padding="6dp"
                        android:src="@android:drawable/ic_menu_search"
                        app:tint="@color/ggram_text_primary"
                        android:contentDescription="Search"
                        android:layout_marginEnd="8dp" />
                </LinearLayout>

            </androidx.appcompat.widget.Toolbar>

            <!-- SMART TABS -->
            <com.google.android.material.tabs.TabLayout
                android:id="@+id/tab_layout"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:background="@color/ggram_surface"
                app:tabGravity="fill"
                app:tabIndicatorColor="@color/ggram_green"
                app:tabIndicatorHeight="3dp"
                app:tabMode="scrollable"
                app:tabSelectedTextColor="@color/ggram_green"
                app:tabTextColor="@color/ggram_text_secondary" />

        </com.google.android.material.appbar.AppBarLayout>

        <!-- CHAT LIST RECYCLER -->
        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipe_refresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_dialogs"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="80dp" />

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <!-- FAB: NEW CHAT / MESSAGE -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_new_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="20dp"
            android:backgroundTint="@color/ggram_green"
            android:src="@android:drawable/ic_menu_edit"
            app:tint="#FFFFFF"
            android:contentDescription="New Message" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- NAVIGATION DRAWER (SIDE MENU) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/ggram_black"
        app:headerLayout="@layout/view_telegram_drawer_header"
        app:itemIconTint="@color/ggram_green"
        app:itemTextColor="@color/ggram_text_primary"
        app:menu="@menu/menu_telegram_drawer" />

</androidx.drawerlayout.widget.DrawerLayout>
