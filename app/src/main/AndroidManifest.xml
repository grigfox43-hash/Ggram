<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <application
        android:name=".GgramApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ggram"
        tools:targetApi="31">

        <!-- 1. Telegram Launch Splash Activity (LAUNCHER) -->
        <activity
            android:name=".ui.launch.TelegramLaunchActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ggram">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Telegram Phone & Code Authentication Activity -->
        <activity
            android:name=".ui.auth.TelegramAuthActivity"
            android:exported="false"
            android:label="Вход в Telegram"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Ggram" />

        <!-- 3. Telegram Main Dialogs / Chats Screen -->
        <activity
            android:name=".ui.dialogs.TelegramDialogsActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ggram" />

        <!-- 4. Telegram Chat Conversation Screen -->
        <activity
            android:name=".ui.chat.TelegramChatActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Ggram" />

        <!-- 5. Ggram Preferences Activity (Accessed via Telegram drawer) -->
        <activity
            android:name=".ui.preferences.GgramPreferencesActivity"
            android:exported="false"
            android:label="@string/ggram_settings_title"
            android:theme="@style/Theme.Ggram" />

        <!-- 6. Message Revision Diff Activity -->
        <activity
            android:name=".ui.diff.GgramDiffActivity"
            android:exported="false"
            android:label="@string/ggram_history_title"
            android:theme="@style/Theme.Ggram" />

        <!-- 7. Calculator Disguise Activity (Double Bottom) -->
        <activity
            android:name=".security.GgramDisguiseActivity"
            android:exported="false"
            android:label="Калькулятор"
            android:theme="@style/Theme.Ggram" />

    </application>

</manifest>
